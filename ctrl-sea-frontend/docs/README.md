# Frontend Notes

The frontend uses Next.js App Router under `src/app`, shared components under `src/components`, and API/auth/type utilities under `src/lib`.

Run:

```powershell
npm run lint
npm run build
```
