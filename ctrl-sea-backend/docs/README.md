# Backend Notes

The backend uses FastAPI routers under `app/api`, service functions under `app/services`, SQLAlchemy models under `app/models`, Pydantic schemas under `app/schemas`, and database setup under `app/database`.

Run:

```powershell
python -m compileall app scripts
```
